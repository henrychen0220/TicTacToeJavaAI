
/**
 * 
 * This is the TicTacToe Game tester class, where
 * we test the TicTacToe logic by creating its class
 * and calling start game
 * 
 */
public class TicTacToeTester {
	/**
	 * create a TicTacToeMain class to begin the game
	 * @param args not used
	 */
	public static void main(String [] args) {
		TicTacToeMain new_game = new TicTacToeMain();
		new_game.startGame();
	}
}
